import React from 'react';
import { Box, Typography } from '@mui/material';

const Header = () => (
  <Box textAlign="center" mt={4}>
    <Typography variant="h3">Bookbot AI Summarizer</Typography>
    <Typography variant="h6">Effortless, accurate book summaries powered by AI</Typography>
  </Box>
);

export default Header;
